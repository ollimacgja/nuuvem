<% flash.each do |type, message| %>
  <div class="alert <%= {success: 'alert-success', notice: 'alert-info', alert: 'alert-danger'}[type.to_sym] || type.to_s %>">
    <button class="close" data-dismiss="alert">×</button>
    <%= message %>
  </div>
<% end %>